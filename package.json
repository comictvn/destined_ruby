
{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "install": "your_custom_install_command",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
}
