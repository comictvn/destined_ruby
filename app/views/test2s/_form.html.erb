<%= form_with(model: test2) do |form| %>
  <% if test2.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(test2.errors.count, "error") %> prohibited this test2 from being saved:</h2>

      <ul>
        <% test2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
