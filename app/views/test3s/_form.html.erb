<%= form_with(model: test3) do |form| %>
  <% if test3.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(test3.errors.count, "error") %> prohibited this test3 from being saved:</h2>

      <ul>
        <% test3.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
